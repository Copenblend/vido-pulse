using System.Windows.Controls;

namespace PulsePlugin.Views;

/// <summary>
/// Pulse sidebar panel — Use Pulse toggle, status, BPM readout, description.
/// </summary>
public partial class PulseSidebarView : UserControl
{
    public PulseSidebarView()
    {
        InitializeComponent();
    }
}
