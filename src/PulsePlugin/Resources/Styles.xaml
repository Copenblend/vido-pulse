<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══ Color Palette (Vido Dark Modern with Pulse accent) ══ -->
    <Color x:Key="PanelBackgroundColor">#1E1E1E</Color>
    <Color x:Key="SurfaceColor">#252526</Color>
    <Color x:Key="BorderColor">#3C3C3C</Color>
    <Color x:Key="TextPrimaryColor">#CCCCCC</Color>
    <Color x:Key="TextSecondaryColor">#808080</Color>
    <Color x:Key="AccentColor">#c42b1c</Color>
    <Color x:Key="BpmTextColor">#DCDCAA</Color>
    <Color x:Key="DisabledColor">#606060</Color>
    <Color x:Key="ToggleOnColor">#c42b1c</Color>
    <Color x:Key="ToggleOffColor">#3C3C3C</Color>
    <Color x:Key="ProgressTrackColor">#3C3C3C</Color>
    <Color x:Key="ProgressFillColor">#c42b1c</Color>
    <Color x:Key="DescriptionBackgroundColor">#1A1A1A</Color>

    <!-- ══ Brushes ══ -->
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="BpmTextBrush" Color="{StaticResource BpmTextColor}" />
    <SolidColorBrush x:Key="DisabledBrush" Color="{StaticResource DisabledColor}" />
    <SolidColorBrush x:Key="ToggleOnBrush" Color="{StaticResource ToggleOnColor}" />
    <SolidColorBrush x:Key="ToggleOffBrush" Color="{StaticResource ToggleOffColor}" />
    <SolidColorBrush x:Key="ProgressTrackBrush" Color="{StaticResource ProgressTrackColor}" />
    <SolidColorBrush x:Key="ProgressFillBrush" Color="{StaticResource ProgressFillColor}" />
    <SolidColorBrush x:Key="DescriptionBackgroundBrush" Color="{StaticResource DescriptionBackgroundColor}" />

    <!-- ══ Section Label ══ -->
    <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <!-- ══ Toggle Switch (Pulse accent) ══ -->
    <Style x:Key="PulseToggleStyle" TargetType="ToggleButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <!-- Track -->
                        <Border x:Name="Track"
                                CornerRadius="9"
                                Background="{StaticResource ToggleOffBrush}" />
                        <!-- Thumb -->
                        <Border x:Name="ThumbContainer"
                                Width="14" Height="14"
                                CornerRadius="7"
                                Background="White"
                                HorizontalAlignment="Left"
                                Margin="2,0,0,0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Background" Value="{StaticResource ToggleOnBrush}" />
                            <Setter TargetName="ThumbContainer" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="ThumbContainer" Property="Margin" Value="0,0,2,0" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Track" Property="Opacity" Value="0.5" />
                            <Setter TargetName="ThumbContainer" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══ Progress Bar ══ -->
    <Style x:Key="PulseProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Height" Value="4" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border x:Name="PART_Track"
                                CornerRadius="2"
                                Background="{StaticResource ProgressTrackBrush}" />
                        <Border x:Name="PART_Indicator"
                                CornerRadius="2"
                                Background="{StaticResource ProgressFillBrush}"
                                HorizontalAlignment="Left" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══ Scrollbar  ══ -->
    <Style x:Key="PulseScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border Background="{StaticResource AccentBrush}"
                            CornerRadius="1"
                            Margin="0" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PulseScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ScrollContentPresenter Grid.Column="0" />
                        <ScrollBar Grid.Column="1"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Width="4" MinWidth="4">
                            <ScrollBar.Style>
                                <Style TargetType="ScrollBar">
                                    <Setter Property="OverridesDefaultStyle" Value="True" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ScrollBar">
                                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                    <Track.Thumb>
                                                        <Thumb Style="{StaticResource PulseScrollBarThumbStyle}" />
                                                    </Track.Thumb>
                                                </Track>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ScrollBar.Style>
                        </ScrollBar>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
